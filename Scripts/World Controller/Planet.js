var NEWTONS_PER_UNIT : int = 9.55;

public class Planet(size, color, type, position) {
  this.size = size;
  this.color = color;
  this.type = type;
  this.position = position;

  this.gravity = size * 9.55;

  function getForceVector(otherPosition) {
    var
  }
}
